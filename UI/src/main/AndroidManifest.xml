<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chess.personal.my.ui">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ChessDotComApp"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Activity.Search">
        <activity android:name=".search.SearchActivity" />
        <activity
            android:name=".search.HomeActivity"
            android:theme="@style/Activity.Home" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".player.PlayerActivity" />
        <activity android:name=".player.PlayerHomeActivity">

        </activity>
        <activity android:name=".club.ClubHomeActivity" />
        <activity android:name=".club.ClubActivity"></activity>
    </application>

</manifest>